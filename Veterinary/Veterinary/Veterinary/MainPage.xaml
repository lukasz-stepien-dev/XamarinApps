<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Veterinary.MainPage">

    <StackLayout BackgroundColor="LightGreen">
        <Label FontSize="32" BackgroundColor="DarkGreen" TextColor="SeaGreen" Text="Wizyta u weterynarza" Padding="10"/>
        <Entry x:Name="OwnerInp" Placeholder="Imię i nazwisko właściciela..."/>
        <Label Text="Gatuenk"/>

        <StackLayout HeightRequest="150">
            <ListView x:Name="Species" ItemTapped="Species_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        
        <StackLayout Orientation="Horizontal">
            <Label Text="Ile ma lat?"/>
            <Label x:Name="AgeLabel" Text=""/>
            <Slider x:Name="AgeSlider" ValueChanged="Slider_ValueChanged" Minimum="0" Maximum="20" WidthRequest="300" />
        </StackLayout>
        <Entry x:Name="PurposeInp" Placeholder="Cel wizyty"/>
        <TimePicker x:Name="TimePicker" Time="16:00"/>
        <Button Text="OK" Clicked="Button_Clicked"/>
    </StackLayout>

</ContentPage>
